<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :actor_id, class: "control-label" %>
    <%= number_input f, :actor_id, class: "form-control" %>
    <%= error_tag f, :actor_id %>
  </div>

  <div class="form-group">
    <%= label f, :receiver_id, class: "control-label" %>
    <%= number_input f, :receiver_id, class: "form-control" %>
    <%= error_tag f, :receiver_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
